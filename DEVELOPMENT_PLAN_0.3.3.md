# TokensAPI Media Gen 插件 0.3.3 开发计划

## 1. 版本目标

本次开发以已发布的 `0.3.2` 为基线，目标版本为 `0.3.3`。

主要目标：

- 新增 `ltx_2_5`、`seedance_2_5` 和 `minimax_h3` 视频模型；
- 保留现有 `ltx_2_3` 和 `seedance_2_0`；
- 根据不同模型动态展示时长、分辨率、图片输入和音频能力；
- 正确区分“强制生成音频”“音频可选”和“音频不可配置”；
- 保持 `0.3.2` 已有的图片下载按钮及其他图片功能不变；
- 完成本地验证后，再决定是否发布至 npm public 和 GitHub。

当前 `0.3.2` 不直接修改或覆盖。开发和测试出现问题时，可以继续使用现有稳定版本。

## 2. 视频模型范围

`0.3.3` 计划支持以下视频模型：

| 模型 | 当前状态 | 计划处理 |
| --- | --- | --- |
| `ltx_2_3` | 开发完成 | 已迁移到统一能力配置，待桌面端回归验证 |
| `seedance_2_0` | 开发完成 | 已迁移到统一能力配置，待桌面端回归验证 |
| `ltx_2_5` | 开发完成 | 模型、参数、固定音频和请求校验已完成，待桌面端验证 |
| `seedance_2_5` | 开发完成 | 推荐参数、音频开关和请求校验已完成，待桌面端验证 |
| `minimax_h3` | 开发完成 | 模型、参数、固定音频和请求校验已完成，待桌面端验证 |

## 3. 已确认的音频规则

音频能力不能继续使用简单的 `supportsAudio: boolean` 表示。计划采用三种状态：

```ts
type AudioMode = 'required' | 'optional' | 'not_configurable'
```

含义如下：

| 状态 | 含义 | 插件交互 |
| --- | --- | --- |
| `required` | 模型默认并强制生成音频 | 显示音频说明，不提供关闭按钮 |
| `optional` | API 允许用户开启或关闭音频 | 显示音频开关 |
| `not_configurable` | API 没有提供可控制的音频参数 | 不显示开关，并明确标记不可配置 |

当前已确认：

| 模型 | 音频模式 | 说明 |
| --- | --- | --- |
| `ltx_2_5` | `required` | 默认且强制生成音频，不允许关闭 |
| `minimax_h3` | `required` | 默认且强制生成音频，不允许关闭 |
| `ltx_2_3` | `required` | 工作流固定生成音频，不提供关闭开关 |
| `seedance_2_0` | `optional` | 默认开启，允许通过 `generate_audio: false` 关闭 |
| `seedance_2_5` | `optional` | 默认开启，允许通过 `generate_audio: false` 关闭 |

## 4. 第一阶段：确认生产参数契约

状态：**已完成（2026-08-28）**。

正式编码前，先确认每个模型的生产接口能力：

- 支持的时长；
- 推荐默认时长；
- 支持的分辨率；
- 推荐默认分辨率；
- 是否支持纯文生视频；
- 是否支持首帧图片；
- 是否支持首帧加尾帧；
- 音频模式及实际请求字段；
- 图片字段、音频字段和其他模型专用字段名称；
- 不同账户类型是否都可使用。

### 4.1 已确认的生产目录

公开价格接口 `https://tokensapi.ai/api/pricing` 返回正常，确认三个新模型均已进入生产目录，支持 `openai-video`，并对 `default`、`vip` 用户组开放。检查时的 `pricing_version` 为：

```text
a42d372ccf0b5dd13ecf71203521f9d2
```

生产价格目录信息：

| 模型 | 时长 | 分辨率 | 备注 |
| --- | --- | --- | --- |
| `ltx_2_5` | 推荐 `5/10` 秒 | `720p/1080p` | `1440p` 不受支持，不加入插件 |
| `seedance_2_5` | 推荐 `5/8/10/15` 秒 | `720p/1080p` | 默认分辨率 `720p` |
| `minimax_h3` | `5/10` 秒 | `480p/720p` | 音频强制开启 |

### 4.2 Gateway 技术契约

| 模型 | Gateway 时长 | Gateway 分辨率 | 输入模式 | 音频 |
| --- | --- | --- | --- | --- |
| `ltx_2_3` | `3/5/8` 秒 | `720p` | 纯文、首帧、首尾帧 | 工作流固定开启 |
| `seedance_2_0` | `4–15` 秒 | `480p/720p/1080p/4k` | 纯文、首帧、首尾帧、参考图、视频编辑 | 可选，默认开启 |
| `ltx_2_5` | 整数 `1–20` 秒 | `720p/1080p` | 纯文、首帧、首尾帧 | 强制开启 |
| `seedance_2_5` | `4–30` 秒或智能时长 `-1` | `480p/720p/1080p` | 纯文、首帧、首尾帧、参考图、视频编辑 | 可选，默认开启 |
| `minimax_h3` | `5/10` 秒 | `480p/720p` | 纯文、首帧、首尾帧 | 强制开启 |

补充约束：

- `ltx_2_5` 和 `minimax_h3` 显式传入 `generate_audio: false` 会在 Gateway 被拒绝；
- Seedance Provider 的 `generate_audio` 默认值为 `true`，同时允许显式传入 `false`；
- `ltx_2_5`、`minimax_h3` 和 Seedance 的尾帧都不能脱离首帧单独使用；
- Seedance 参考图不能与首帧或尾帧组合使用；
- `seedance_2_5` 的首帧任务要求使用 `adaptive` 画面比例；
- `seedance_2_5` 技术契约还支持参考视频、参考音频、MOV 输出和高级 Omni 任务，但不属于 `0.3.3` 首次接入范围。

### 4.3 插件 0.3.3 开放范围

插件不直接暴露 Gateway 的全部技术范围，而采用生产价格目录中的推荐参数，减少未定价组合和契约漂移风险。

| 模型 | 插件时长选项 | 插件分辨率选项 | 插件输入模式 | 音频交互 |
| --- | --- | --- | --- | --- |
| `ltx_2_3` | `3/5/8` 秒 | `720p` | 纯文、首帧、首尾帧 | 显示“自动生成音频”，不允许关闭 |
| `seedance_2_0` | `5/8/10/15` 秒 | `480p/720p/1080p` | 纯文、首帧、首尾帧 | 显示音频开关，默认开启 |
| `ltx_2_5` | `5/10` 秒 | `720p/1080p` | 纯文、首帧、首尾帧 | 显示“自动生成音频”，不允许关闭 |
| `seedance_2_5` | `5/8/10/15` 秒 | `720p/1080p` | 纯文、首帧、首尾帧 | 显示音频开关，默认开启 |
| `minimax_h3` | `5/10` 秒 | `480p/720p` | 纯文、首帧、首尾帧 | 显示“自动生成音频”，不允许关闭 |

`ltx_2_5` 明确只开放 `720p` 和 `1080p`。`1440p` 当前不受支持，不加入 `0.3.3`，也不列为本版本的后续开放项。

Seedance 2.5 的任意 `4–30` 秒、智能时长、`480p`、参考图、参考视频、参考音频、视频编辑和 MOV 输出暂不进入 `0.3.3` 的基础交互，以控制本次版本范围。

本阶段只使用公开生产目录和本地代码进行只读确认，没有创建生成任务，也没有产生费用。后续实际创建视频任务会产生费用，执行前需要单独确认。

## 5. 第二阶段：建立统一模型能力表

计划在 `src/shared/media.ts` 中增加统一的视频模型能力配置，替代当前散落在向导和生成工具中的模型判断。

建议结构：

```ts
type VideoInputMode = 'text' | 'first_frame' | 'first_last_frame'
type AudioMode = 'required' | 'optional' | 'not_configurable'

interface VideoModelCapability {
  durations: readonly number[]
  resolutions: readonly string[]
  aspectRatios: readonly string[]
  defaultDuration: number
  defaultResolution?: string
  defaultAudioEnabled?: boolean
  audioMode: AudioMode
  inputModes: readonly VideoInputMode[]
}

const VIDEO_MODEL_CAPABILITIES = {
  // 每个视频模型的能力定义
} as const
```

该配置将统一驱动：

- 插件支持的视频模型列表；
- 默认模型配置；
- 向导中的时长选项；
- 向导中的分辨率选项；
- 首帧、尾帧输入方式；
- 音频交互和说明；
- 生成工具参数校验；
- TokensAPI 请求体构造；
- 最终确认摘要；
- 自动化测试。

## 6. 第三阶段：改造视频交互向导

选择模型后，向导根据模型能力动态展示参数，不再使用固定的 LTX/Seedance 条件分支。

### 6.1 模型选择

模型列表预计包含：

```text
ltx_2_3
ltx_2_5
seedance_2_0
seedance_2_5
minimax_h3
```

最终固定展示顺序调整为：

```text
minimax_h3
ltx_2_5
ltx_2_3
seedance_2_5
seedance_2_0
```

`defaultVideoModel` 默认设为 `ltx_2_5`。默认模型保留普通的“插件默认”说明，但不显示绿色“推荐”标记，也不会改变固定展示顺序。

### 6.2 动态输出参数

选择模型后，仅展示该模型支持的：

- 视频生成方式；
- 时长；
- 分辨率；
- 画面比例；
- 音频选项或音频状态说明。

### 6.3 音频交互

以 `ltx_2_5` 为例，最终界面应表达为：

```text
模型：ltx_2_5
时长：5 秒
分辨率：1080p
音频：自动生成（该模型要求开启）
```

处理规则：

- `required`：只显示说明，不提供关闭按钮；
- `optional`：显示开启或关闭选项；
- `not_configurable`：显示“音频由模型工作流决定”或不显示音频控制项；
- 不再显示容易误解的“音频不支持”。

### 6.4 最终确认摘要

创建任务前的摘要应至少包含：

- 任务类型；
- Prompt 和增强状态；
- 模型；
- 图片输入方式；
- 时长；
- 分辨率；
- 画面比例；
- 音频状态。

## 7. 第四阶段：生成工具和请求校验

更新视频生成工具 Schema 和执行逻辑：

- 接受新增的三个模型；
- 根据所选模型校验时长；
- 根据所选模型校验分辨率；
- 根据所选模型校验首帧和尾帧组合；
- 阻止向模型发送不支持的字段；
- 在创建付费任务前拦截无效参数；
- 保持当前图片上传、任务轮询、视频保存和播放逻辑不变。

音频请求处理规则：

- `required`：按照生产接口契约保持音频开启；
- 如果用户或 Agent 传入 `generate_audio: false`，在提交任务前直接报错；
- `optional`：仅在用户明确选择时发送音频字段；
- `not_configurable`：不向 API 发送未经支持的音频参数。

## 8. 第五阶段：自动化测试

需要补充以下测试：

### 8.1 模型注册测试

- 五个视频模型均出现在插件模型列表；
- `defaultVideoModel` 可以配置为任一受支持模型；
- 无效模型会在提交任务前被拒绝。

### 8.2 参数能力测试

- 每个模型只接受自身支持的时长；
- 每个模型只接受自身支持的分辨率；
- 不支持首尾帧的模型不能接收相关字段；
- 尾帧不能在没有首帧的情况下单独提交。

### 8.3 音频测试

- `ltx_2_5` 不能关闭音频；
- `minimax_h3` 不能关闭音频；
- `ltx_2_3` 不发送关闭音频的参数，并显示工作流固定开启状态；
- 最终确认摘要显示正确的音频状态。

### 8.4 回归测试

- 图片生成正常；
- 图片编辑正常；
- `0.3.2` 新增的图片下载图标仍然存在；
- 视频本地保存和播放器不受影响；
- 超时任务恢复功能不受影响；
- Client bundle 仍使用正确的包名注册 ModuleLoader。

## 9. 第六阶段：构建和本地验证

代码完成后依次执行：

```bash
npm run typecheck
npm test
npm run build
npm run pack:dry
```

全部通过后生成本地安装包：

```text
tokensapi-dsh-media-gen-0.3.3.tgz
```

本地验证顺序：

1. 保留当前可用版本和回滚信息；
2. 安装本地 `0.3.3` 包；
3. 完全重启 TokensCowork；
4. 检查插件能否正常加载；
5. 检查五个模型是否显示；
6. 检查不同模型的动态参数弹窗；
7. 检查音频状态文案；
8. 检查图片下载按钮；
9. 在获得确认后执行必要的实际生成测试。

安装、重启和实际任务测试均属于有状态操作。每一步执行前需要说明影响、风险和回滚方法，完成后立即汇报结果。

## 10. 第七阶段：发布

只有本地验证通过后才进入发布阶段：

1. 将 `package.json` 版本更新为 `0.3.3`；
2. 更新中英文 README；
3. 更新 CHANGELOG；
4. 检查 npm 包文件列表；
5. 确认安装包不包含 API Key、Token、密码或个人配置；
6. 创建符合规范的 Git 提交；
7. 推送至 GitHub；
8. 发布 npm public；
9. 验证 npm latest 和插件市场安装；
10. 在另一套 TokensCowork 环境验证安装兼容性。

GitHub 推送和 npm 发布需要分别获得明确确认，不因“开始开发”而自动执行。

## 11. 计划修改的主要文件

| 文件 | 计划修改内容 |
| --- | --- |
| `src/shared/media.ts` | 视频模型列表、能力类型和统一能力配置 |
| `src/host/index.ts` | 向导交互、参数校验、Schema、请求体和确认摘要 |
| `src/host/types.ts` | 视频模型及音频能力相关类型 |
| `test/smoke.test.mjs` | 模型注册、Client bundle 和回归检查 |
| 新增或拆分的测试文件 | 能力矩阵、向导、参数和请求体测试 |
| `README.md` | 英文功能和参数说明 |
| `README.zh.md` | 中文功能和参数说明 |
| `CHANGELOG.md` | `0.3.3` 更新记录 |
| `package.json` | 发布阶段才更新版本号 |

## 12. 验收标准

`0.3.3` 必须同时满足以下条件才可以发布：

- 五个视频模型正确显示；
- 每个模型只显示和接受自身支持的参数；
- LTX 2.5 和 MiniMax H3 正确标记为强制音频；
- LTX 2.3 不再被错误描述为“不支持音频”；
- Seedance 2.5 的音频和图片输入能力已经过生产契约确认；
- 所有无效参数都在创建付费任务前被拦截；
- 图片下载图标和原有图片功能没有回归；
- TypeScript 检查、自动化测试、构建和打包检查全部通过；
- 本地 TokensCowork 安装和插件加载正常；
- 文档、版本号、npm 包内容和 GitHub 代码保持一致。
