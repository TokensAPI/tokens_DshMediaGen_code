# Register @tokensapi/dsh-media-gen with schema defaults.
# Override outputDir, default models, storage, or polling settings per machine when needed.
- insert:
    - id: media-gen
      name: '@tokensapi/dsh-media-gen'
